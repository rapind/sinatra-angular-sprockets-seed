# More info at https://github.com/guard/guard#readme

guard 'livereload' do
  # App
  watch(%r{app.rb})

  # Asset (Sprocket) files
  watch(%r{assets\/.*})

  # Static files
  watch(%r{public\/.*})
end